<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTOMapper3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.ProviderMapper">

	<select id="readProviderList" resultType="com.example.demo.bean.ProviderVO">
		SELECT * 
		FROM provider
		ORDER BY id
	</select>
</mapper>